clc;
clear all;
close all;
t = (-4:0.01:4)';
unitstep = t>=0;
impulse = t==0;
u1=t>=-0.5;
u2=t>=0.5;
ramp=t.*unitstep;
u3=t>=-1;
u4=t>=1;
r1=(t+1).*u3;
r2=(t-1).*u4;
u5=t>=-5.5/6;
u6=2*u5;
u7=t>=0.5/6;
u8=2*u7;
u9=t>=-2.5/6;
u10=2*u9;
r3=(2*t+11/6).*u6;
r4=(2*t+5/6).*u10;
r5=(2*t-1/6).*u8;
figure;
subplot(3,2,1);
plot(t,sin(2*t)+sin(sqrt(2)*t));
grid on;
xlabel('sin(root(2))t+sin(2t))');
subplot(3,2,1);
plot(t,sin(2*t)+sin(sqrt(2)*t));
grid on;
xlabel('sin(root(2))t+sin(2t))');
subplot(3,2,2);
plot(3*cos(2*pi*t+pi/3).*unitstep);
grid on;
xlabel('3cos(2*pi*t+pi/3)*u(t)');
subplot(3,2,3);
plot(t,(u1-u2));
grid on;
xlabel('u(t+0.5)-u(t-0.5)');
subplot(3,2,4);
plot(t,r1-2*ramp+r2);
grid on;
xlabel('r(t-1)+2r(t)+r(t+1)');
subplot(3,2,5);
plot(t,r3-2*r4+r5);
grid on;
xlabel('tri(2t+5/6)');